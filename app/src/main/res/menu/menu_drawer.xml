<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/home"
        android:icon="@drawable/ic_home"
        android:title="الرئيسيه"
        android:checked="true"
        app:cnb_iconColor="@color/colorPrimary"/>

    <item
        android:id="@+id/rides"
        android:icon="@drawable/ic_updates"
        android:title="السابقه"
        android:checked="true"
        app:cnb_iconColor="@color/colorBlack"/>

    <item
        android:id="@+id/settings"
        android:icon="@drawable/ic_settings"
        android:title="الاعدادات"
        app:cnb_iconColor="@color/colorGreen"/>
</menu>